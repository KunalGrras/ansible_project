---
- name: Install Apache
  hosts: all
  become: yes
  vars: 
    - package: httpd
    - states: present
  tasks:
    - name: Install httpd
      yum:
        name: "{{ package }}"
        state: "{{ states }}"
