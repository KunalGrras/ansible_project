---
- name: Install Apache
  hosts: all
  become: yes
  vars: 
    - name: httpd
    - state: present
  tasks:
    - name: Install httpd
      yum:
        name: "{{ package }}"
        state: "{{ states }}"
